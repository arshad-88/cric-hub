.env.local
node_modules
dist

# Convex codegen output — committed so a fresh checkout builds without a login
# (Convex docs: "your code won't typecheck without it"). Regenerated by
# `npx convex dev`; keep the committed copy in sync with the schema.
!src/convex/_generated
